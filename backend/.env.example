# Groq API key used by the SQL Agent. Keep the real value in backend/.env.
GROQ_API_KEY=your_groq_api_key

# Use "production" to hide detailed CSV parser errors from API responses.
DATAMIND_ENV=development
